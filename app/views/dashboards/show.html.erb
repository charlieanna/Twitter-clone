<%= form_for @dashboard.new_tweet do |f| %>
  <%= f.text_field :status %>
  <%= f.submit "Create Tweet" %>
<% end %>
<%=render @dashboard.timeline %>


<ul class="tweets" ng-controller="TweetsCtrl" ng-init="get('<%= event.id%>');subscribe_as_admin('<%=event.code%>',<%= current_admin.present?%>)">

 
</ul>

